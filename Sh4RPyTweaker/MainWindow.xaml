<Window x:Class="Sh4RPyTweaker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sh4RPyTweaker" Height="740" Width="1180"
        MinHeight="620" MinWidth="980"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BgBrush}"
        Foreground="{StaticResource PrimaryTextBrush}"
        FontFamily="Segoe UI" FontSize="13"
        UseLayoutRounding="True"
        TextOptions.TextFormattingMode="Display">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="236" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- ======== Боковая панель ======== -->
        <Border Grid.Column="0" Background="{StaticResource SidebarBrush}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="22,22,22,20">
                    <TextBlock Text="Sh4RPyTweaker" FontSize="24" FontWeight="Bold" Foreground="White" />
                    <TextBlock Text="Настройка Windows" FontSize="12" Foreground="{StaticResource SecondaryTextBrush}" Margin="0,2,0,0" />
                </StackPanel>

                <ListBox x:Name="NavList" Grid.Row="1"
                         Style="{StaticResource NavListStyle}"
                         ItemContainerStyle="{StaticResource NavItemStyle}"
                         SelectionChanged="NavList_SelectionChanged">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Glyph}" FontFamily="Segoe MDL2 Assets"
                                           FontSize="16" Margin="0,0,12,0" VerticalAlignment="Center" />
                                <TextBlock Text="{Binding Title}" FontSize="13.5" VerticalAlignment="Center" />
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <Border Grid.Row="2" Margin="16,12,16,16" CornerRadius="8" Background="#202020" Padding="10">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <Ellipse Width="8" Height="8" Fill="{StaticResource SuccessBrush}" VerticalAlignment="Center" Margin="0,0,8,0" />
                            <TextBlock Text="Права администратора" FontSize="11.5" Foreground="{StaticResource SecondaryTextBrush}" />
                        </StackPanel>
                        <TextBlock Text="Изменения применяются мгновенно" FontSize="11" Foreground="{StaticResource SecondaryTextBrush}" Margin="0,4,0,0" />
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- ======== Контент ======== -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Padding="24,14" Background="{StaticResource BgBrush}"
                    BorderBrush="{StaticResource HeaderBorderBrush}" BorderThickness="0,0,0,1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="PageTitle" FontSize="20" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center" />
                    <Button Grid.Column="1" Content="Резервная копия" Style="{StaticResource SecondaryButtonStyle}"
                            Click="BackupButton_Click" />
                </Grid>
            </Border>

            <ScrollViewer x:Name="ContentScroll" Grid.Row="1"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          Padding="24,20">
                <ContentControl x:Name="ContentHost" Focusable="False" />
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>

